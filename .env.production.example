# Configuration de l'environnement Lootopia - PRODUCTION

# IMPORTANT: Ne jamais committer les vraies valeurs de production !
# Copier ce fichier en .env.production et remplir avec les vraies valeurs

# Environnement
NODE_ENV=production

# Base de données MariaDB
DB_HOST=database
DB_PORT=3306
DB_DATABASE=lootopia
DB_USER=lootopia_user
DB_PASSWORD=CHANGEZ_MOI_AVEC_UN_MOT_DE_PASSE_FORT
DB_ROOT_PASSWORD=CHANGEZ_MOI_AVEC_UN_MOT_DE_PASSE_FORT_ROOT

# Backend API
BACKEND_PORT=3000

# Frontend
FRONTEND_PORT=80

# JWT Configuration - CRITIQUE : Utilisez une clé secrète forte et unique !
# Générez avec : openssl rand -base64 32
JWT_SECRET=CHANGEZ_MOI_AVEC_UNE_CLE_SECRETE_FORTE_ET_UNIQUE
JWT_EXPIRATION=3600

# Bcrypt rounds (plus élevé = plus sécurisé mais plus lent)
BCRYPT_ROUNDS=12

# CORS - Mettre le domaine de production
CORS_ORIGIN=https://votre-domaine.com
