# Backend Environment Variables

NODE_ENV=development
PORT=3000
API_PREFIX=api

# Database (MariaDB)
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=lootopia
DB_USERNAME=lootopia_user
DB_PASSWORD=lootopia_password
DB_SYNCHRONIZE=true
DB_LOGGING=true

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:4200
